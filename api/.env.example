# RexLit API Configuration Example
# Copy this file to .env and adjust paths as needed

# REXLIT_HOME - Root directory for API security boundary
# Documents must be within this path to be accessible via API
# For development: use project root to keep documents, index, and audit together
REXLIT_HOME=/Users/benjaminginsberg/dev/r/rex

# REXLIT_DATA_DIR - Python CLI data directory (should match REXLIT_HOME)
# This is passed to CLI subprocess for index/audit operations
REXLIT_DATA_DIR=/Users/benjaminginsberg/dev/r/rex

# REXLIT_BIN - Path to rexlit CLI executable
# Auto-detected from .venv/bin/rexlit by default
# REXLIT_BIN=/Users/benjaminginsberg/dev/r/rex/.venv/bin/rexlit

# PORT - API server port
PORT=3000

# NOTE: After changing REXLIT_HOME or REXLIT_DATA_DIR:
# 1. Rebuild the index: rexlit index build juul_docs --workers 4 --rebuild
# 2. Restart the API server
# 3. Verify: curl http://localhost:3000/api/cli-status
